import { RegistryService } from "./services/registry";

RegistryService.setItem("logging", true);               // Allows console logging
RegistryService.setItem("allowZoom", false);            // Allows zooming with mouse wheel
RegistryService.setItem("zoomAmount", 1.157135714);     // Sets the zoom amount
RegistryService.setItem("zoomToggleKey", "t");          // Sets the key used for toggling zoom
